version: '3.8'
services:
  ekadhara:
    build:
      context: ./26145
      dockerfile: Dockerfile
    container_name: ekadhara
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
